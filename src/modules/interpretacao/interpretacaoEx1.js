export const interpretacaoEx1 = () => {

    // Exercício de Interpretação de Código
    console.log(`O que faz:
Conversão de 100 dólares para reais de acordo com a cotação informada em um prompt.

Como faz:
Através de uma função que recebe o valor em dólares como argumento e retorna o produto entre o valor em dólares e a cotação para reais informada em um prompt.

Console:
prompt(2) = R$ 200
prompt(5) = R$ 500
prompt(abc) = NaN`)
}